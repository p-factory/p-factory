import React from 'react';
import Template from '../../global/components/Template.test';
import LoginComponent from './Login.component';

const Login = () => {
  return <Template component={LoginComponent} />;
};

export default Login;
